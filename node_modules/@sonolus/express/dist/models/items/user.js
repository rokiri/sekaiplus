import { toTags } from '../tag.js';
export const toUserItem = (sonolus, localize, item) => ({
    name: item.name,
    source: sonolus.address,
    title: localize(item.title),
    handle: item.handle,
    tags: toTags(localize, item.tags),
});
