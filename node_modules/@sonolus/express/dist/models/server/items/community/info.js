import { toServerForms } from '../../forms/form.js';
import { toServerItemCommunityComment, } from './comments/comment.js';
export const toServerItemCommunityInfo = (sonolus, localize, info, actions, commentActions) => ({
    actions: toServerForms(localize, info.actions, actions),
    topComments: info.topComments.map((comment) => toServerItemCommunityComment(sonolus, localize, comment, commentActions)),
});
