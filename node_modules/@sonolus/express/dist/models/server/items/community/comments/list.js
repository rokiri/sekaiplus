import { toServerItemCommunityComment } from './comment.js';
export const toServerItemCommunityCommentList = (sonolus, localize, list, actions) => ({
    pageCount: list.pageCount,
    cursor: list.cursor,
    comments: list.comments.map((comment) => toServerItemCommunityComment(sonolus, localize, comment, actions)),
});
