import { toServerForms } from '../../forms/form.js';
export const toServerLevelResultInfo = (localize, info, submits) => ({
    submits: info.submits && toServerForms(localize, info.submits, submits),
});
