export const toServerItemLeaderboard = (localize, leaderboard) => ({
    name: leaderboard.name,
    title: localize(leaderboard.title),
    description: leaderboard.description && localize(leaderboard.description),
});
