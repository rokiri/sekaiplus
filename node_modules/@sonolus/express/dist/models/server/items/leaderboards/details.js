import { toServerItemLeaderboardRecord, } from './records/record.js';
export const toServerItemLeaderboardDetails = (sonolus, localize, details) => ({
    topRecords: details.topRecords.map((record) => toServerItemLeaderboardRecord(sonolus, localize, record)),
});
