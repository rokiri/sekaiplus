import { toServerItemLeaderboardRecord } from './record.js';
export const toServerItemLeaderboardRecordList = (sonolus, localize, list) => ({
    pageCount: list.pageCount,
    cursor: list.cursor,
    records: list.records.map((record) => toServerItemLeaderboardRecord(sonolus, localize, record)),
});
