import { toReplayItem } from '../../../../items/replay.js';
export const toServerItemLeaderboardRecordDetails = (sonolus, localize, details) => ({
    replays: details.replays.map((replay) => toReplayItem(sonolus, localize, replay)),
});
